<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Vinyl Records Shop{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
            <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Raleway:wght@300;400;500;700&display=swap">
            <style>
                :root {
                    --primary-beige: #d9d4c6;
                    --primary-black: #18181b;
                    --primary-light: #ffffff;
                    --primary-beige-light: #e8e5dc;
                    --primary-beige-dark: #c5bfb0;
                    --secondary-accent: #a67c52;
                }
                
                body {
                    min-height: 100vh;
                    display: flex;
                    flex-direction: column;
                    font-family: 'Raleway', sans-serif;
                    color: var(--primary-black);
                    background-color: var(--primary-light);
                    margin: 0;
                    padding: 0;
                }
                
                h1, h2, h3, h4, h5, h6 {
                    font-family: 'Playfair Display', serif;
                }
                
                .content-wrapper {
                    flex: 1;
                }
                
                .navbar-brand span {
                    font-weight: bold;
                    font-family: 'Playfair Display', serif;
                    letter-spacing: 0.5px;
                }
                
                .record-icon {
                    animation: spin 4s linear infinite;
                    display: inline-block;
                    margin-right: 5px;
                    transition: transform 0.2s;
                }
                
                @keyframes spin {
                    from { transform: rotate(0deg); }
                    to { transform: rotate(360deg); }
                }
                
                .btn-primary {
                    background-color: var(--primary-beige);
                    border-color: var(--primary-beige-dark);
                    color: var(--primary-black);
                }
                
                .btn-primary:hover, .btn-primary:focus {
                    background-color: var(--primary-beige-dark);
                    border-color: var(--primary-beige-dark);
                    color: var(--primary-black);
                }
                
                .btn-outline-primary {
                    border-color: var(--primary-beige);
                    color: var(--primary-black);
                }
                
                .btn-outline-primary:hover, .btn-outline-primary:focus {
                    background-color: var(--primary-beige);
                    border-color: var(--primary-beige);
                    color: var(--primary-black);
                }
                
                .product-card {
                    transition: all 0.3s ease;
                    border: none;
                    border-radius: 8px;
                    overflow: hidden;
                    box-shadow: 0 4px 6px rgba(0,0,0,0.05);
                }
                
                .product-card:hover {
                    transform: translateY(-5px);
                    box-shadow: 0 10px 20px rgba(0,0,0,0.1);
                }
                
                .product-card .card-body {
                    padding: 1.5rem;
                }
                
                .card {
                    border-radius: 8px;
                    border: none;
                    box-shadow: 0 4px 6px rgba(0,0,0,0.05);
                }
                
                .card-header {
                    background-color: var(--primary-beige);
                    color: var(--primary-black);
                    font-weight: 500;
                }
                
                .footer {
                    margin-top: auto;
                    padding: 20px 0;
                    background-color: var(--primary-black);
                    color: var(--primary-light);
                }
                
                .table-hover tbody tr:hover {
                    background-color: var(--primary-beige-light);
                }
                
                .alert-success {
                    background-color: #d4e6d9;
                    border-color: #c1d9c4;
                    color: #0f5132;
                }
                
                .alert-danger {
                    background-color: #f8d7da;
                    border-color: #f5c2c7;
                    color: #842029;
                }
                
                .form-control:focus {
                    border-color: var(--primary-beige);
                    box-shadow: 0 0 0 0.25rem rgba(217, 212, 198, 0.25);
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {% include 'partials/navbar.html.twig' %}

        <div class="content-wrapper" style="background-color: var(--primary-light); flex: 1 0 auto; width: 100%;">
            <div class="container mt-4 mb-4">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    {% endfor %}
                {% endfor %}

                {% block body %}{% endblock %}
            </div>
        </div>

        <footer style="background-color: var(--primary-black); color: var(--primary-light); padding: 3rem 0 1.5rem;">
            <div class="container">
                <div class="d-flex align-items-center mb-4">
                    <div style="flex-grow: 1; height: 1px; background-color: var(--primary-beige);"></div>
                    <h3 class="mx-4 mb-0" style="color: var(--primary-beige); font-family: 'Playfair Display', serif;">Vinyl Records Shop</h3>
                    <div style="flex-grow: 1; height: 1px; background-color: var(--primary-beige);"></div>
                </div>
                
                <div class="row py-4">
                    <div class="col-md-4 mb-4">
                        <h5 style="font-family: 'Playfair Display', serif; color: var(--primary-beige);">About Us</h5>
                        <p class="mt-3">Your premier destination for vinyl records. Browse our extensive collection of classic and contemporary albums, carefully curated for audiophiles and music lovers.</p>
                        <div class="mt-3">
                            <a href="#" class="me-2" style="color: var(--primary-beige);"><i class="bi bi-facebook fs-5"></i></a>
                            <a href="#" class="me-2" style="color: var(--primary-beige);"><i class="bi bi-instagram fs-5"></i></a>
                            <a href="#" class="me-2" style="color: var(--primary-beige);"><i class="bi bi-twitter-x fs-5"></i></a>
                        </div>
                    </div>
                    <div class="col-md-4 mb-4">
                        <h5 style="font-family: 'Playfair Display', serif; color: var(--primary-beige);">Quick Links</h5>
                        <ul class="list-unstyled mt-3">
                            <li class="mb-2"><a href="{{ path('app_home') }}" class="text-decoration-none" style="color: var(--primary-light);"><i class="bi bi-chevron-right me-1" style="color: var(--primary-beige);"></i> Home</a></li>
                            <li class="mb-2"><a href="{{ path('app_products') }}" class="text-decoration-none" style="color: var(--primary-light);"><i class="bi bi-chevron-right me-1" style="color: var(--primary-beige);"></i> Products</a></li>
                            {% if app.user %}
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li class="mb-2"><a href="{{ path('app_admin_products') }}" class="text-decoration-none" style="color: var(--primary-light);"><i class="bi bi-chevron-right me-1" style="color: var(--primary-beige);"></i> Manage Products</a></li>
                                    <li class="mb-2"><a href="{{ path('app_admin_orders') }}" class="text-decoration-none" style="color: var(--primary-light);"><i class="bi bi-chevron-right me-1" style="color: var(--primary-beige);"></i> All Orders</a></li>
                                {% else %}
                                    <li class="mb-2"><a href="{{ path('app_cart') }}" class="text-decoration-none" style="color: var(--primary-light);"><i class="bi bi-chevron-right me-1" style="color: var(--primary-beige);"></i> Cart</a></li>
                                    <li class="mb-2"><a href="{{ path('app_my_orders') }}" class="text-decoration-none" style="color: var(--primary-light);"><i class="bi bi-chevron-right me-1" style="color: var(--primary-beige);"></i> My Orders</a></li>
                                {% endif %}
                            {% else %}
                                <li class="mb-2"><a href="{{ path('app_login') }}" class="text-decoration-none" style="color: var(--primary-light);"><i class="bi bi-chevron-right me-1" style="color: var(--primary-beige);"></i> Login</a></li>
                                <li class="mb-2"><a href="{{ path('app_register') }}" class="text-decoration-none" style="color: var(--primary-light);"><i class="bi bi-chevron-right me-1" style="color: var(--primary-beige);"></i> Register</a></li>
                            {% endif %}
                        </ul>
                    </div>
                    <div class="col-md-4 mb-4">
                        <h5 style="font-family: 'Playfair Display', serif; color: var(--primary-beige);">Contact Us</h5>
                        <ul class="list-unstyled mt-3">
                            <li class="mb-2"><i class="bi bi-envelope me-2" style="color: var(--primary-beige);"></i> contact@vinylshop.com</li>
                            <li class="mb-2"><i class="bi bi-telephone me-2" style="color: var(--primary-beige);"></i> +1 (555) 123-4567</li>
                            <li class="mb-2"><i class="bi bi-geo-alt me-2" style="color: var(--primary-beige);"></i> 123 Music Street, Vinyl City</li>
                        </ul>
                    </div>
                </div>
                <div style="height: 1px; background-color: var(--primary-beige-dark); opacity: 0.5;" class="mb-3"></div>
                <div class="text-center py-3" style="color: var(--primary-light); opacity: 0.8;">
                    <p class="mb-0">&copy; {{ 'now'|date('Y') }} Vinyl Records Shop. All rights reserved.</p>
                </div>
            </div>
        </footer>
    </body>
</html>
