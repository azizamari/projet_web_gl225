<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Vinyl Records Shop{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
            <style>
                body {
                    min-height: 100vh;
                    display: flex;
                    flex-direction: column;
                }
                .content-wrapper {
                    flex: 1;
                }
                .navbar-brand span {
                    font-weight: bold;
                }
                .record-icon {
                    animation: spin 4s linear infinite;
                    display: inline-block;
                    margin-right: 5px;
                    transition: transform 0.2s;
                }
                @keyframes spin {
                    from { transform: rotate(0deg); }
                    to { transform: rotate(360deg); }
                }
                .product-card {
                    transition: transform 0.3s;
                }
                .product-card:hover {
                    transform: translateY(-5px);
                    box-shadow: 0 10px 20px rgba(0,0,0,0.1);
                }
                .footer {
                    margin-top: auto;
                    padding: 20px 0;
                    background-color: #212529;
                    color: #fff;
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {% include 'partials/navbar.html.twig' %}

        <div class="content-wrapper">
            <div class="container mt-4 mb-4">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    {% endfor %}
                {% endfor %}

                {% block body %}{% endblock %}
            </div>
        </div>

        <footer class="footer">
            <div class="container">
                <div class="row">
                    <div class="col-md-4">
                        <h5>Vinyl Records Shop</h5>
                        <p>Your premier destination for vinyl records. Browse our extensive collection of classic and contemporary albums.</p>
                    </div>
                    <div class="col-md-4">
                        <h5>Quick Links</h5>
                        <ul class="list-unstyled">
                            <li><a href="{{ path('app_home') }}" class="text-white">Home</a></li>
                            <li><a href="{{ path('app_products') }}" class="text-white">Products</a></li>
                            {% if app.user %}
                                <li><a href="{{ path('app_cart') }}" class="text-white">Cart</a></li>
                                <li><a href="{{ path('app_my_orders') }}" class="text-white">My Orders</a></li>
                            {% else %}
                                <li><a href="{{ path('app_login') }}" class="text-white">Login</a></li>
                                <li><a href="{{ path('app_register') }}" class="text-white">Register</a></li>
                            {% endif %}
                        </ul>
                    </div>
                    <div class="col-md-4">
                        <h5>Contact Us</h5>
                        <p><i class="bi bi-envelope"></i> contact@vinylshop.com</p>
                        <p><i class="bi bi-telephone"></i> +1 (555) 123-4567</p>
                        <p><i class="bi bi-geo-alt"></i> 123 Music Street, Vinyl City</p>
                    </div>
                </div>
                <div class="text-center mt-3">
                    <p>&copy; {{ 'now'|date('Y') }} Vinyl Records Shop. All rights reserved.</p>
                </div>
            </div>
        </footer>
    </body>
</html>
